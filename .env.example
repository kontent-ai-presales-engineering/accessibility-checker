# Environment Variables

# Optional: Enable verbose logging for debugging
# Set to 'true' to see detailed logs for Kontent.ai integration
# Default: false (only shows warnings and errors)
VITE_VERBOSE_LOGGING=false

# Note: When deployed as a Kontent.ai custom app, the Management API key
# is provided through the custom app configuration, not as an env variable
